# The stack's namespace, with the same Pod Security admission the
# production namespace of deploy/bootstrap carries: a stack that admitted
# more than the real thing would prove less than the real thing.
apiVersion: v1
kind: Namespace
metadata:
  name: arca
  labels:
    pod-security.kubernetes.io/enforce: restricted
    pod-security.kubernetes.io/warn: restricted
